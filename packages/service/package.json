{
  "name": "@mcp-mail/service",
  "version": "0.1.0",
  "description": "REST API proxy service for MCP Mail (SMTP/IMAP relay)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "echo 'Bun runs directly, no build needed'",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "zod": "^3.24.0",
    "imap": "^0.8.19",
    "mailparser": "^3.7.4",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/imap": "^0.8.42",
    "@types/mailparser": "^3.4.6",
    "@types/nodemailer": "^6.4.0",
    "typescript": "^5.8.2"
  }
}