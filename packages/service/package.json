{
  "name": "@mcp-mail/service",
  "version": "0.1.0",
  "description": "REST API proxy service for MCP Mail (SMTP/IMAP relay)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "echo 'Bun runs directly, no build needed'",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@mcp-mail/core": "file:../core",
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.2"
  }
}